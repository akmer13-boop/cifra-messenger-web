# Отчёт проверки CIFRA Web v22

Дата проверки: 29 июля 2026.

## Результат

- `npm ci` — успешно на фиксированном lock-файле;
- ESLint — без ошибок;
- 49 из 49 WEB-тестов — успешно;
- TypeScript — без ошибок;
- `next build` — успешно;
- статический экспорт `out/index.html` — создан;
- `/health.json` — `status=ok`, версия `1.2.0-web-v22`;
- production dependency audit — 0 известных уязвимостей;
- Tailwind/PostCSS dependency отсутствует, поэтому
  `@tailwindcss/postcss` не требуется.

Проверены отдельные контракты MFA/RBAC, обязательная смена временного пароля,
серверное назначение роли, read-only права модератора, статусы сообщений,
актуальный preview и поднятие чата по последней активности.

Активный runtime в этом архиве — `demo`, чтобы Amvera смогла открыть сайт без
доступа к локальной сети компании. Для реального Gateway используйте второй,
интеграционный архив и инструкцию `LOCAL_SERVER_RUNBOOK_RU.md`.
